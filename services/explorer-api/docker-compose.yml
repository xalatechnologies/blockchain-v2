version: '3.8'

services:
  api:
    build: .
    container_name: norchain-api
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - RPC_URL=${RPC_URL:-https://rpc.xaheen.org}
      - CHAIN_ID=${CHAIN_ID:-65001}
      - ENABLE_CACHE=true
      - RATE_LIMIT_MAX_REQUESTS=100
      - API_KEY_ENABLED=${API_KEY_ENABLED:-false}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
    networks:
      - norchain-network

  # PostgreSQL Database
  postgres:
    image: postgres:14-alpine
    container_name: norchain-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-norchain_explorer}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./src/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    restart: unless-stopped
    networks:
      - norchain-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis for distributed caching
  redis:
    image: redis:7-alpine
    container_name: norchain-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    networks:
      - norchain-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Blockchain Indexer Service
  indexer:
    build: .
    container_name: norchain-indexer
    command: node src/services/indexer-service.js
    environment:
      - NODE_ENV=production
      - RPC_URL=${RPC_URL:-https://rpc.xaheen.org}
      - CHAIN_ID=${CHAIN_ID:-65001}
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=${DB_NAME:-norchain_explorer}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-postgres}
      - INDEXER_SYNC_INTERVAL=${INDEXER_SYNC_INTERVAL:-3000}
      - INDEXER_BATCH_SIZE=${INDEXER_BATCH_SIZE:-100}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - norchain-network

volumes:
  postgres-data:
  redis-data:

networks:
  norchain-network:
    driver: bridge

